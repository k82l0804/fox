# API keys and secrets — never commit these
openai-proxy/.env
gitlab/.env
.env

# Node / build artifacts (for future CLI and ACP client work)
node_modules/
dist/
out/
*.js.map

# VSCode extension packaging
*.vsix

# OS noise
.DS_Store
Thumbs.db

# Upstream reference repositories (local clones only)
ext-repo/
ext-rep/

